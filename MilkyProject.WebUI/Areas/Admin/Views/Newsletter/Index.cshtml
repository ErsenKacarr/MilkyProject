@model List<MilkyProject.WebUI.Dtos.NewsletterDtos.ResultNewsletterDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-wrapper">
    <div class="page-header">
        <h3 class="page-title">Haber</h3>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
            </ol>
        </nav>
    </div>
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Haberler</h4>
                    <p class="card-description">
                        Aşağıdaki tablodan haberler ile ilgili bilgiler üzerinde işlem yapabilirsiniz.
                    </p>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>E Mail</th>
                                    <th>Sil</th>
                                    <th>Güncelle</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.newsletterid</td>
                                        <td>@item.email</td>
                                        <td><a href="/Admin/Newsletter/DeleteNewsletter/@item.newsletterid" class="btn btn-danger">Sil</a></td>
                                        <td><a href="/Admin/Newsletter/UpdateNewsletter/@item.newsletterid" class="btn btn-success">Güncelle</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/Admin/Newsletter/CreateNewsletter" class="btn btn-primary">Yeni Haber Girişi</a>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
